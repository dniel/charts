apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-posts.fullname" . }}-test-connection"
  labels:
{{ include "api-posts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "api-posts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
