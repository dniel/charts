apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-graphql.fullname" . }}
  labels:
    {{- include "api-graphql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-graphql.selectorLabels" . | nindent 4 }}
